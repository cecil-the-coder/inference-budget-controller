apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - daemonset.yaml

# Image tag override — set via `kustomize edit set image` or --load-restrictor
images:
  - name: ghcr.io/cecil-the-coder/fox-operator
    newTag: latest
